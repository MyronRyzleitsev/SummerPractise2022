#ifndef HEADER_H
#define HEADER_H
#include <stdio.h>
#include <locale.h>
#include <string.h>
#include <stdlib.h>
#include <windows.h>

void two_arrays(float num, int uah[], int pen[]);

void rename(int num, const int arr[]);

#endif