#ifndef HEADER_H
#define HEADER_H
#include <stdio.h>
#include <locale.h>
#include <string.h>
#include <stdlib.h>
#include <windows.h>

void two_arrays(double num, int uah[], int pen[]);

void rename(double num, const int arr[]);

#endif